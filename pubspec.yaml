name: medicine_reminder
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  # Bloc State Management
  bloc: ^9.0.0
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7
  # Other Packages
  mobile_scanner: ^6.0.6
  numberpicker: ^2.1.2
  # Local Packages
  app_core:
    path: packages/app_core
  svg_icon:
    path: packages/svg_icon

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/pictures/svg/
